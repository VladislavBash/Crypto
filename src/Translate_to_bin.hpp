#include <vector>
#include <string>
extern std::vector<int> bit_trans(std::string opText, std::string lang, int n);
extern std::string collapse(std::string col, int n);
extern std::string bit_untrans(std::string clText, std::string lang, int n);